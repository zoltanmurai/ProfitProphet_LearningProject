<Window x:Class="ProfitProphet.Views.StrategyTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="http://oxyplot.org/wpf"
        xmlns:local="clr-namespace:ProfitProphet.Views"
        mc:Ignorable="d"
        Title="Stratégia Tesztelő" 
        Height="720" Width="1100"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        Background="#1E1E1E">

    <Window.Resources>
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="#252526"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/> <ColumnDefinition Width="*"/>   </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#252526" BorderBrush="#3E3E42" BorderThickness="0,0,1,0" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

                <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Vezérlőpult" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>

                        <TextBlock Text="{Binding CurrentProfile.Name}" Foreground="#007ACC" FontWeight="Bold" FontSize="15" TextWrapping="Wrap" Margin="0,0,0,2"/>
                        <TextBlock Text="{Binding Symbol}" Foreground="Gray" FontWeight="SemiBold" Margin="0,0,0,10"/>

                        <Button Command="{Binding EditStrategyCommand}" 
                                Content="Stratégia tervező ✏️" 
                                Height="28" Margin="0,0,0,15"
                                Background="#3E3E42" Foreground="White" BorderThickness="0"/>

                        <Separator Background="#3E3E42" Margin="0,0,0,15"/>

                        <TextBlock Text="Időszak Kezdete:" Foreground="Gray" Margin="0,0,0,2"/>
                        <DatePicker SelectedDate="{Binding StartDate}" Margin="0,0,0,8" Background="#333" Foreground="Black"/>

                        <TextBlock Text="Időszak Vége:" Foreground="Gray" Margin="0,0,0,2"/>
                        <DatePicker SelectedDate="{Binding EndDate}" Margin="0,0,0,15" Background="#333" Foreground="Black"/>

                        <GroupBox Header="Pénzügyek és Jutalékok" Foreground="LightGray" BorderBrush="#444" Margin="0,0,0,15">
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="110"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Kezdő Tőke ($):" VerticalAlignment="Center" Foreground="Gray" Margin="0,0,0,4"/>
                                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding InitialCash}" Background="#333" Foreground="White" BorderBrush="#555" Padding="2" Margin="0,0,0,4"/>

                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Kötés Típus:" VerticalAlignment="Center" Foreground="Gray" Margin="0,0,0,4"/>
                                <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding TradeAmountTypes}" SelectedItem="{Binding CurrentProfile.AmountType}" Margin="0,0,0,4"/>

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Kötés Méret:" VerticalAlignment="Center" Foreground="Gray" Margin="0,0,0,4"/>
                                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding CurrentProfile.TradeAmount}" ToolTip="Darabszám vagy Fix összeg ($)" Background="#333" Foreground="White" BorderBrush="#555" Padding="2" Margin="0,0,0,4"/>

                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Jutalék (%):" VerticalAlignment="Center" Foreground="Gray" Margin="0,0,0,4"/>
                                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding CurrentProfile.CommissionPercent}" ToolTip="Pl. 0.45" Background="#333" Foreground="White" BorderBrush="#555" Padding="2" Margin="0,0,0,4"/>

                                <TextBlock Grid.Row="4" Grid.Column="0" Text="Min. Díj ($):" VerticalAlignment="Center" Foreground="Gray" Margin="0,0,0,4"/>
                                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding CurrentProfile.MinCommission}" ToolTip="Minimum fizetendő díj (pl. 7 USD)" Background="#333" Foreground="White" BorderBrush="#555" Padding="2" Margin="0,0,0,4"/>

                                <TextBlock Grid.Row="5" Grid.Column="0" Text="Napi Swap ($):" VerticalAlignment="Center" Foreground="Gray"/>
                                <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding CurrentProfile.DailySwap}" ToolTip="Naponta levont költség pozíciónként" Background="#333" Foreground="White" BorderBrush="#555" Padding="2"/>
                            </Grid>
                        </GroupBox>

                        <Border Background="#2D2D30" CornerRadius="4" Padding="8" Margin="0,0,0,10">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <CheckBox IsChecked="{Binding UseOptimization}" VerticalAlignment="Center">
                                        <CheckBox.LayoutTransform>
                                            <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                                        </CheckBox.LayoutTransform>
                                    </CheckBox>
                                    <TextBlock Text="Optimalizálás" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="White" FontWeight="SemiBold"/>
                                </StackPanel>

                                <Button Command="{Binding OpenOptimizerCommand}" 
                                    Content="Beállítások ⚙️" 
                                    Height="28" 
                                    IsEnabled="{Binding UseOptimization}"
                                    BorderThickness="0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="#444"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>

                                            <Style.Triggers>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Background" Value="#252526"/>
                                                    <Setter Property="Foreground" Value="#555"/>
                                                </Trigger>

                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#555"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <Button Grid.Row="1" 
                        Command="{Binding RunCommand}" 
                        Content="FUTTATÁS ▶" 
                        Height="45" 
                        Background="#007ACC" Foreground="White" FontWeight="Bold" FontSize="16" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>    <RowDefinition Height="Auto"/>  <RowDefinition Height="1.5*"/>  </Grid.RowDefinitions>

            <Border Grid.Row="0" BorderBrush="#3E3E42" BorderThickness="1" Background="#1E1E1E" Margin="0,0,0,5">
                <oxy:PlotView Model="{Binding EquityModel}" Background="Transparent"/>
            </Border>

            <StackPanel Grid.Row="1" Margin="0,5,0,10">
                <TextBox Text="{Binding IndicatorTestValues}" 
                         IsReadOnly="True" TextWrapping="Wrap"
                         Background="#252526" Foreground="#FFD700" BorderBrush="#3E3E42" 
                         Padding="6" FontSize="12" FontFamily="Consolas"/>
            </StackPanel>

            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="Eredmények Listája" Foreground="#CCCCCC" FontWeight="Bold" Margin="0,0,0,5"/>

                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding OptimizationResults}" 
                          SelectedItem="{Binding SelectedResult}"
                          AutoGenerateColumns="False" 
                          IsReadOnly="True"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          Background="#1E1E1E" RowBackground="#252526" AlternatingRowBackground="#2D2D30"
                          Foreground="White" BorderBrush="#3E3E42" GridLinesVisibility="None" HeadersVisibility="Column">

                    <DataGrid.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick" 
                                      Command="{Binding ApplyResultCommand}" 
                                      CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=DataGrid}, Path=SelectedItem}"/>
                    </DataGrid.InputBindings>

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#3E3E42"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Padding" Value="8,5"/>
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                            <Setter Property="BorderBrush" Value="#222"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                            <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#007ACC"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Profit ($)" Binding="{Binding Profit, StringFormat=N0}" Width="100" FontWeight="Bold">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Kötés" Binding="{Binding TradeCount}" Width="60">
                             <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Drawdown" Binding="{Binding Drawdown, StringFormat=P1}" Width="80">
                             <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="Foreground" Value="#FF6666"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Score" Binding="{Binding Score, StringFormat=N2}" Width="80">
                             <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="Foreground" Value="#FFA500"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Paraméterek" Binding="{Binding ParameterSummary}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>
    </Grid>
</Window>