<Window x:Class="ProfitProphet.Views.OptimizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Stratégia Optimalizálás Beállítása" Height="500" Width="700" 
        Background="#1E1E1E" Foreground="#E0E0E0"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#333337"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Optimalizációs Tartományok" FontSize="20" FontWeight="Bold" Foreground="#007ACC"/>
            <TextBlock Text="Válaszd ki, mely paramétereket akarod optimalizálni, és add meg a határokat." 
                       FontSize="12" Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#3F3F46" BorderThickness="1" Background="#252526" Padding="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding AvailableParameters}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#3F3F46" BorderThickness="0,0,0,1" Padding="5,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="200"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="60"/>
                                    </Grid.ColumnDefinitions>

                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="1" Text="{Binding Name}" 
                                               VerticalAlignment="Center" FontWeight="SemiBold" 
                                               TextTrimming="CharacterEllipsis" ToolTip="{Binding Name}"/>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="(" Foreground="Gray"/>
                                        <TextBlock Text="{Binding CurrentValue}" Foreground="#FFD700"/>
                                        <TextBlock Text=")" Foreground="Gray"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="3" Text="Min:" VerticalAlignment="Center" Foreground="Gray" HorizontalAlignment="Right" Margin="0,0,5,0"/>
                                    <TextBox Grid.Column="4" Text="{Binding MinValue}" IsEnabled="{Binding IsSelected}"/>

                                    <TextBlock Grid.Column="5" Text="Max:" VerticalAlignment="Center" Foreground="Gray" HorizontalAlignment="Right" Margin="0,0,5,0"/>
                                    <TextBox Grid.Column="6" Text="{Binding MaxValue}" IsEnabled="{Binding IsSelected}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Mégse" Command="{Binding CancelCommand}" 
                    Width="100" Height="30" Margin="0,0,10,0" 
                    Background="#444" Foreground="White"/>

            <Button Content="BEÁLLÍTÁS KÉSZ (OK)" Command="{Binding OkCommand}" 
                    Width="150" Height="30" 
                    Background="#007ACC" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>