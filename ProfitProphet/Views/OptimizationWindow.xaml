<Window x:Class="ProfitProphet.Views.OptimizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:ProfitProphet.Converters"
        mc:Ignorable="d"
        Title="Stratégia Optimalizálás" Height="600" Width="900" 
        Background="#1E1E1E" Foreground="#E0E0E0"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:IsPositiveConverter x:Key="IsPositiveConverter"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#333337"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Optimalizációs Paraméterek" FontSize="20" FontWeight="Bold" Foreground="#007ACC"/>
            <TextBlock Text="Jelöld ki a változókat és add meg a keresési tartományt." FontSize="12" Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#3F3F46" BorderThickness="1" Background="#252526" Padding="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding AvailableParameters}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#3F3F46" BorderThickness="0,0,0,1" Padding="5,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="200"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="60"/>
                                    </Grid.ColumnDefinitions>

                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" 
                                              VerticalAlignment="Center"
                                              IsEnabled="{Binding DataContext.IsNotRunning, RelativeSource={RelativeSource AncestorType=Window}}"/>

                                    <TextBlock Grid.Column="1" Text="{Binding Name}" 
                                               VerticalAlignment="Center" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding Name}"/>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="(" Foreground="Gray"/>
                                        <TextBlock Text="{Binding CurrentValue}" Foreground="#FFD700"/>
                                        <TextBlock Text=")" Foreground="Gray"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="3" Text="Min:" VerticalAlignment="Center" Foreground="Gray" HorizontalAlignment="Right" Margin="0,0,5,0"/>
                                    <TextBox Grid.Column="4" Text="{Binding MinValue}" IsEnabled="{Binding IsSelected}"/>

                                    <TextBlock Grid.Column="5" Text="Max:" VerticalAlignment="Center" Foreground="Gray" HorizontalAlignment="Right" Margin="0,0,5,0"/>
                                    <TextBox Grid.Column="6" Text="{Binding MaxValue}" IsEnabled="{Binding IsSelected}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="2" Margin="0,15,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="{Binding StatusText}" Foreground="#007ACC" FontWeight="Bold" Margin="0,0,0,5"/>
                <ProgressBar Height="10" 
                             Value="{Binding ProgressValue}" Maximum="100"
                             IsIndeterminate="{Binding IsRunning}"
                             Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}"
                             BorderThickness="0" Background="#333"/>
            </StackPanel>

            <Button Grid.Column="1" Content="OPTIMALIZÁLÁS FUTTATÁSA" 
                    Command="{Binding RunOptimizationCommand}"
                    Width="200" Height="40" 
                    Background="#007ACC" Foreground="White" FontWeight="Bold"
                    BorderThickness="0" Margin="10,0,0,0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="3"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>

        <DataGrid Grid.Row="3" ItemsSource="{Binding OptimizationResults}" 
                  AutoGenerateColumns="False" IsReadOnly="True" 
                  Background="#252526" Foreground="White" RowBackground="#2D2D30"
                  AlternatingRowBackground="#333337" GridLinesVisibility="None"
                  HeadersVisibility="Column" BorderThickness="0">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#3E3E42"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="5"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Paraméterek" Binding="{Binding ParameterSummary}" Width="*"/>

                <DataGridTextColumn Header="Profit" Binding="{Binding Profit, StringFormat={}{0:N0} $}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Profit, Converter={StaticResource IsPositiveConverter}}" Value="True">
                                    <Setter Property="Foreground" Value="LightGreen"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Kötés" Binding="{Binding TradeCount}" Width="50"/>

                <DataGridTextColumn Header="Drawdown" Binding="{Binding Drawdown, StringFormat=P1}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#FF6666"/>
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Score" Binding="{Binding Score, StringFormat=N2}" Width="60" FontWeight="Bold"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>